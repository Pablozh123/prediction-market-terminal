feat: real auth — st.login() + Google OIDC, fail-closed Settings gate, fake shell removed

Roadmap #4 (LAUNCH_PLAN §3). Without .streamlit/secrets.toml [auth] the app
is unchanged: no login surface, Settings open (local research mode). With
auth secrets configured, the sidebar gains Sign in/Sign out (st.login/
st.logout) and the Settings page fails closed: only signed-in accounts on
the admin allowlist (ADMIN_EMAILS env, else [admin].emails in secrets) may
manage configuration; every research workspace stays public.

- app/authz.py (Streamlit-free, 19 tests): provider detection from the
  [auth] section (flat default vs [auth.<name>] subsection), e-mail
  normalization, env-over-secrets allowlist, settings_access decision
  matrix (open/login_required/no_allowlist/not_allowed/ok).
- prediction_terminal.py: current_auth_provider/current_user_email/
  settings_admin_emails/trigger_login wrappers, sidebar account block,
  render_settings_gate + gate at the top of page_settings. Removed the
  fake auth dialog, /sign-in & /sign-up shell routes, sidebar Sign In/
  Sign Up buttons, auth_dialog_mode state, and .auth-note CSS.
- src/prediction_markets.py: drop local_auth_route_mode (fake-shell-only).
- .streamlit/secrets.toml.example: Google OIDC walkthrough + [admin]
  allowlist template; secrets.toml git- and docker-ignored; commented
  read-only mount in docker-compose.yml; ADMIN_EMAILS in .env.example.
- requirements.txt: Authlib (st.login dependency).
- Tests: tests/test_authz.py (19) + AuthGateSmokeTests (Settings open
  without secrets, gated with secrets + anonymous user); smoke scripts
  drop the removed shell routes. 259 unit tests green; full AppTest page
  smoke 16/16; Playwright visual smoke 11 routes on a temp 8504 server.
- Docs: HANDOFF (#4 done, new §7, renumbering, next steps), LAUNCH_PLAN §3
  implemented note, PRODUCTION_READINESS admin-protection checked, README
  auth setup section.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
