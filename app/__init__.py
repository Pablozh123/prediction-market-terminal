"""App-layer helpers for the prediction terminal.

Pure, Streamlit-free modules extracted from ``prediction_terminal.py`` so the
presentation/filter logic can be imported and unit-tested in isolation.
"""
